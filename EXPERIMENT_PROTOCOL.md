# بروتوكول التجربة العليا

## 1. سؤال التجربة

هل يمكن إطلاق مشروع برمجي حقيقي من نقطة ابتدائية محدودة، ثم ترك منظومة ذكاء اصطناعي تديره وتطوره ذاتيًا عبر دورات PDSA متعاقبة، دون تدخل بشري تطويري، إلى أن تنتج مرشحًا نهائيًا قابلًا للاستخدام يحقق تعريف الإنجاز المعتمد؟

## 2. وحدة التجربة

المستودع `My_Website` هو الحمولة التجريبية. المنتج المطلوب منه هو موقع ويب لمفتش التكوين والتعليم المهنيين. نجاح الموقع وحده لا يكفي للحكم على التجربة؛ يجب أيضًا قياس كيفية الوصول إليه ومدى الاستقلالية الفعلية.

## 3. لحظة الإطلاق

لا تبدأ التجربة أثناء مرحلة `BOOTSTRAP`.

تبدأ التجربة فقط عند تحقق الشروط التالية معًا:

1. اجتياز اختبارات بنية الحوكمة والتحكم.
2. ضبط مزود ذكاء اصطناعي صالح للاستعمال الآلي.
3. نجاح دورة تجريبية تقنية لا تعد من دورات المنتج.
4. تثبيت ملفات الحوكمة والبروتوكول.
5. تحويل حالة المشروع صراحة إلى `ACTIVE` وتسجيل طابع زمني للبداية.

## 4. الاستقلالية المطلوبة بعد الإطلاق

بعد الإطلاق، لا يجوز للإنسان أو لمساعد خارجي أن يقوم نيابة عن النظام بأي من الأعمال التالية:

- تحديد الهدف المرحلي التالي.
- تصميم خطة التنفيذ.
- اختيار المعمارية أو التقنية أو المكتبات.
- كتابة أو تصحيح كود المنتج.
- حل مشكلة تطويرية أو معرفية للمشروع.
- تحديد أولويات الميزات.
- تغيير نتائج المراجعة أو الدراسة.
- تجاوز فشل تحقق من أجل استمرار التطوير.

يجب أن تتخذ المنظومة هذه القرارات بنفسها ضمن القيود الثابتة.

## 5. التدخل البشري المسموح

يسمح بالتدخل فقط لاستعادة بنية تحتية خارجية لازمة للتجربة، مثل:

- انتهاء أو تعطل مفتاح API.
- إلغاء مزود خارجي أو تغيير واجهته.
- عطل في GitHub Actions أو الصلاحيات.
- عطل يمنع المنظومة من التشغيل أصلًا ولا يتعلق بقرار تطويري للمنتج.

كل تدخل من هذا النوع يجب أن يسجل في سجل التدخلات، مع السبب والوقت ونطاق التغيير، ويرفع عداد `human_infrastructure_interventions`.

إذا احتاج المشروع إلى قرار بشري تطويري لكي يتقدم، فذلك نتيجة تجريبية مهمة ولا يجوز إخفاؤها بتقديم القرار له.

## 6. المتغير التجريبي الأساسي

`Human Development Intervention Count (HDIC)`

القيمة المثالية بين `START` و`FINAL_CANDIDATE` هي صفر.

يجب الفصل بين:

- `HDIC`: تدخل بشري في التطوير أو القرار.
- `HIIC`: تدخل بشري لاستعادة البنية التحتية فقط.

## 7. مؤشرات المتابعة

تسجل المنظومة على الأقل:

- عدد دورات PDSA المكتملة.
- عدد الخطط المرفوضة قبل الاعتماد.
- عدد مراجعات الخطة.
- أخطاء التنفيذ.
- حالات فشل التحقق.
- التراجعات أو Rollbacks.
- المشاكل المتكررة بين الدورات.
- عدد مرات تعذر اتخاذ قرار ذاتي.
- استدعاءات الذكاء الاصطناعي والنماذج المستخدمة.
- استهلاك الرموز إن أتاحه المزود.
- مدة التطوير التقويمية.
- HDIC وHIIC.

## 8. معيار الوصول إلى المنتج النهائي

لا يعلن النظام النجاح لمجرد وجود موقع يعمل. عندما يرى أن `DEFINITION_OF_DONE.md` تحقق، يحول الحالة إلى `FINAL_AUDIT_PENDING`.

تجرى بعد ذلك مراجعة نهائية مستقلة عن دور التنفيذ. إذا اجتازت جميع الشروط، تصبح الحالة `FINAL_CANDIDATE`.

بعدها فقط تبدأ تجربة المستخدم البشري. بيانات تجربة المستخدم تعامل كمدخلات تطوير جديدة، لا كأوامر مباشرة لتعديل المنتج.

## 9. عدم التلاعب بالنتيجة

ملفات هذا البروتوكول والحوكمة والبنية التي تفرضها لا يجوز لوكيل المنتج تعديلها بعد الإطلاق. إذا ظهر قصور فيها يسجل كقيد أو عطل في التجربة؛ ولا يصلح أثناء التشغيل إلا ضمن تدخل بنية تحتية موثق إذا كان استمرار التجربة مستحيلًا بدونه.

## 10. مبدأ عدم تحسين نتيجة التجربة

على الوكلاء العاملين داخل المشروع تحسين المنتج وفق هدفه وقواعد PDSA فقط. لا يجوز لهم تحسين المؤشرات التجريبية عمدًا أو إعلان النجاح من أجل خفض HDIC أو تقليل عدد الدورات. المنتج هو المهمة؛ قياس الاستقلالية يتم من الخارج عبر السجل.
